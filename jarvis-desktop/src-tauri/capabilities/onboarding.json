{
  "$schema": "../gen/schemas/windows-schema.json",
  "identifier": "onboarding",
  "description": "The first-run walkthrough: three static screens and a close button. `jarvis-link` is granted only for its read-only `get_theme`, the same as every other window, so this page paints the owner's actual theme rather than always opening in the default one. It cannot read the event stream, the approval queue, memory, or anything else Jarvis does — the only write it is capable of is marking itself seen.",
  "windows": [
    "onboarding"
  ],
  "permissions": [
    "core:app:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:path:default",
    "core:webview:default",
    "core:window:default",
    "jarvis-link",
    "onboarding-surface"
  ]
}
