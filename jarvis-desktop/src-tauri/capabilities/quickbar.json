{
  "$schema": "../gen/schemas/windows-schema.json",
  "identifier": "quickbar",
  "description": "The Alt+Space spotlight. Streams chat, renders approval gates, shows the daily brief, records the microphone on push-to-talk and plays back a spoken reply, and owns its own window geometry. It cannot reach the connection settings or backend supervision.",
  "windows": [
    "quickbar"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:webview:allow-set-webview-zoom",
    "core:webview:allow-internal-toggle-devtools",
    "jarvis-link",
    "approvals",
    "task-control",
    "quickbar-surface",
    "memory-used",
    "attention-surface",
    "voice"
  ]
}
