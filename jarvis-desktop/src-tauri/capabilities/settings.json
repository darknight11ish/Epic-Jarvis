{
  "$schema": "../gen/schemas/windows-schema.json",
  "identifier": "settings",
  "description": "The settings window, and the only surface that can write anything. It holds the two privileged commands: set_supervision persists a program that start_backend runs, and set_api_settings redirects the base URL every token-bearing request is built from. This window is created on demand and does not exist until the user opens it.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "jarvis-link",
    "settings-surface"
  ]
}
